<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
            xmlns:app="http://schemas.android.com/apk/res-auto"
            xmlns:tools="http://schemas.android.com/tools"
            android:id="@+id/nav_graph"
            app:startDestination="@id/entryFragment">

    <fragment
        android:id="@+id/entryFragment"
        android:name="com.schibsted.elephant.android.ui.entry.EntryFragment"
        android:label="Entry"
        tools:layout="@layout/fragment_entry"/>

    <fragment
            android:id="@+id/leaderbordFragment"
            android:name="com.schibsted.elephant.android.leaderboard.LeaderboardFragment"
            android:label="Leaderboard"
            tools:layout="@layout/fragment_leaderboard"/>

    <action android:id="@+id/actionToLeaderbord"
            app:destination="@id/leaderbordFragment"/>
</navigation>
